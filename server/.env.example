MONGO_URI=mongodb+srv://<usuario>:<senha>@<cluster>.mongodb.net/finance-tracker?retryWrites=true&w=majority
PORT=5000

# Gere cada segredo com: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
# Precisam ser DIFERENTES entre si.
JWT_ACCESS_SECRET=<segredo-aleatorio-longo>
JWT_REFRESH_SECRET=<outro-segredo-aleatorio-longo>
